# tmux-sessionizer — projects.txt
# Each line is a directory path or a glob pattern.
# Blank lines and lines starting with # are ignored.
# Environment variables and ~ are expanded.

# ── exact directories ──────────────────────────────────────────────────────────
~/.config/nvim
~/Documents/MyPkm

# ── glob patterns (all first-level subdirs) ────────────────────────────────────
~/Repos/*
~/.local/share/nvim/lazy/*
~/.config/*
~/dotfiles/*
